# 2026-09-22-迭代0-素材拆帧与导入

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-22 |
| 迭代目标 | 把 H5 原型用的横向精灵条与 UI 素材拆成逐帧 PNG 并导入工程，定死导入设置与锚点换算 |
| 关联计划 | `Docs/Plans/2026-09-22-计划-兵种系统与美术落地.md` |

## 关键步骤

1. **写拆帧工具** `Docs/Design/_tools/split-frames.ps1`：读 `Docs/Design/h5/assets/` 下的精灵条，按帧宽切分，输出到 `Assets/Art/`。帧数表沿用 H5 的 `assets.js`（unit idle 8 / walk 6 / atk 5~12 / death 6；enemy walk 9 或 8、atk 9~16、death 6~14）。

2. **产出锚点换算表**：脚本同时输出 `Assets/Art/Manifest.csv`，逐帧记录 `frameW/frameH/refH/worldHeight/scale/offsetY`。这是「离线加工」与「编辑器批量导入」之间唯一的桥 —— 1007 个资产的参数不能散在人脑里。

   换算严格等价于 H5 `play/js/assets.js` 的 `drawSprite`：
   ```
   scale   = worldHeight × 100 / refH        # PPU 取 100，尺寸由 SO 的 scale 决定
   offsetY = (frameH / 100 × scale − worldHeight) / 2
   ```

3. **单图资源直接复制**：UI 零件 182 + 窗口 6 + 图标 17 + 背景 2、道具 15、头像 4、地形 52。

4. **批量设置导入参数**：`texture_set_settings_batch` 一次提交 1007 项，21.8 秒完成，0 失败。

5. **给 `Assets/Art/` 加保护性 `.gitignore`**：本仓库当前是**公开**的，而素材许可（策划案 R11）未读 —— 当初 `Docs/Design/h5/assets/` 正是为避免公开再分发才从全部历史中摘除。已告知用户，用户正在把仓库改为私有；**确认后需删掉该忽略行并提交素材**。

## 设计决策

- **拆帧而非切片**：`unity-skills` 没有切图操作（`spriteMode=Multiple` 能设，但定义子精灵矩形没有对应技能），而手写 `.meta` 在本引擎上必然失败 —— 该引擎的 `.meta` guid 被混淆过，与 YAML 里引用的 hex guid 无推导关系。
- **`filterMode` 保持默认 Bilinear，不用 Point**：素材是手绘风不是像素风，Point 会让边缘锯齿。真正必须改的是 `textureType`（默认是 Default，不改就没法当精灵用）。
- **`PPU = 100` 统一值 + 尺寸交给 SO 的 scale**：`sprite_set_import_settings` 没有批量版，若逐个设 PPU 需要 1007 次 REST 调用（外加 1007 次重导）。改成「统一 PPU + SO 控尺寸」后，导入只需 3 次批量调用。
- **不做离线重采样（见下方遗留说明）**：先走通管线，重采样留作优化。

## 影响范围

- 新增：`Assets/Art/`（1007 个 PNG + 1007 个 `.meta` + `Manifest.csv`，43MB）
- 新增：`Docs/Design/_tools/split-frames.ps1`、`gen-import-payload.ps1`
- 修改：`.gitignore`（新增 `Assets/Art/` 保护性忽略）
- 未改动任何脚本、预制体、场景

## 验证结果

- ✅ **拆帧完整**：1007 张逐帧 PNG；`Units` 238、`Enemies` 288、`Vfx` 19、`UI` 391、`Items` 15、`Portraits` 4、`Tiles` 52
- ✅ **单帧切分正确**：抽查 `Units/man/gun_atk/002.png` —— 干净的单帧，无相邻帧串入
- ✅ **`.meta` 全部配对**：1007 PNG / 1007 `.meta`，无缺失（丢了 `.meta` 会被重算 GUID，预制体与场景引用全断）
- ✅ **导入设置全部生效**：抽 3 个文件核对，均为 `textureType=Sprite` / `spriteMode=Single` / `compression=Uncompressed` / `mipmaps=false` / `PPU=100`
- ✅ **尺寸与清单一致**：`Units/man/gun_idle/000.png` 实测 112×135，与 Manifest 记录一致
- ✅ **批量导入 1007/1007 成功，0 失败**
- ✅ **素材不会误入公开仓库**：`git add -A --dry-run` 扫到的 `Assets/Art` 文件数为 **0**

**锚点换算的关键断言（这是「攻击时人物缩小」那条修复的 Unity 版）：**

| 检查 | 结果 |
|------|------|
| 同一角色各动画 `scale` 完全一致 | `man` 全部帧 `scale = 1.2963` ✓ |
| `offsetY` 随帧高变化 | idle 0 / walk −0.019 / 攻击 +0.447 / 球棍攻击 +0.830 / 死亡 −0.091 ✓ |
| 敌人尺寸各自不同（BOSS 应更大） | z1 `0.643` / z7 `1.0` / BOSS1 `1.619` / 巨型 `2.37` ✓ |

## 遗留说明

**显存占用偏高。** 实测原始像素 42.5 Mpx，未压缩 RGBA32 约 **170MB 显存**。

> ⚠ **本文初版写的「可降 4~6 倍」是错的 —— 没算过就拍的。** 2026-09-24 实测后修正如下：

按 1080p 下的屏幕密度（相机 ortho size 5 → 视野高 10 单位 → **108 像素/世界单位**）逐角色核对：

| 组 | 像素 | 占比 | 源密度 | 能否缩 |
|----|------|------|--------|--------|
| Enemies | 21.4 Mpx | 50% | z1 156 / mega 42 px/单位 | 只有 z1~z8 过采样 |
| UI | 12.4 Mpx | 29% | 1:1 屏显 | 否 |
| Units | 7.6 Mpx | 18% | man 77 / girl 99 px/单位 | 否（已偏欠采样） |
| 其余 | 1.1 Mpx | 3% | — | 否 |

- **man（77）与 mega（42）是欠采样** —— 屏幕上需 108 才够清晰，缩小只会更糊
- 真正过采样的只有 **z1~z8 那 8 种小怪**（156 px/单位，过采样 1.44×）
- **实际可省约 15~20%，不是 4~6 倍**

**而且迭代 2「同屏 80 敌」的真正瓶颈不是显存，是 draw call** —— 80 个敌人各用一张独立贴图 = 80 次 draw call，无法合批。重采样解决不了这个，需要 **SpriteAtlas**（`com.unity.2d.sprite` 尚不在 manifest，见计划风险 R13）。

**2026-09-24 决定：先不做重采样**，优先做 SpriteAtlas。

修法备查：在 `split-frames.ps1` 里按角色乘重采样系数（只缩不放），或额外把每帧裁到内容包围盒（后者收益更大，但要改 `CharacterSpriteSet` 支持逐帧锚点）。**代价是所有 `scale`/`offsetY` 要重算重导**，所以越晚改越贵。

**其他遗留**：
- 工程仍无中文字体（策划案 R6），迭代 1 的兵种选择界面会立刻撞上
- 仓库仍是公开的，`Assets/Art/` 处于忽略状态；改为私有后需移除忽略行并单独提交素材

## 下一步状态

迭代 0 完成。**下一步是迭代 1：兵种数据层 + 玩家换美术**，内容包括：
- 四个实体加 `Visual` 子节点、删 Cube 三件套（`MeshFilter`/`MeshRenderer`/`MeshCollider` 要一起删）
- `Assets/Configs/` 建 `BodyDefinition` / `WeaponDefinition` / `CharacterSpriteSet` / `ClassCatalog` / `GameConfig`
- `CharacterView` 脚本逐帧动画（不用 Animator）
- `PlayerStatSystem` + `GameArchitecture` 注册第 5 项 + `StartNewGameCommand` 补第 7 步复位

按「一个迭代一个闭环」约定，**等你确认后再开始**。

> 另需你决定：是否现在补做离线重采样（见上「遗留说明」）。
