# 2026-09-07-迭代4-局内升级三选一

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-07 |
| 迭代目标 | 局内升级三选一 |
| 关联计划 | `Docs/Plans/2026-09-07-计划-菌核狂潮MVP架构与迭代计划.md` |

## 关键步骤

1. 扩展 `GameStateModel`：加 `Level`（`BindableProperty<int>`）。
2. 新增 `UpgradeSystem`（ISystem）：监听经验变化，达标触发升级，生成三选一（攻击力 / 攻速 / 移速）。
3. 新增 `ChooseUpgradeCommand`：把选中升级应用到玩家表现层组件。
4. 新增 `UpgradePanelController`：程序化创建 UI（Canvas + 3 按钮），订阅 `Level` 变化展示面板，点击发命令应用。
5. 改造 `PlayerCombatController`（伤害/攻速字段 + `AddDamage`/`IncreaseFireRate`）、`ProjectileController`（伤害经 `Init` 注入）、`PlayerController`（`AddMoveSpeed`）。
6. 注册 `UpgradeSystem` 到架构。

## 影响范围

- 新增 `System/UpgradeSystem.cs`、`Command/ChooseUpgradeCommand.cs`、`Controller/UpgradePanelController.cs`
- 修改 `Model/GameStateModel.cs`、`Controller/{PlayerCombatController,ProjectileController,PlayerController}.cs`、`GameArchitecture.cs`

## 验证结果

- ✅ 编译通过（0 错误）。
- ✅ 经验拾取 → 升级触发（Console 出现「升级到等级 2/3」）。
- ✅ 升级面板弹出（`Panel activeSelf=True`，展示三选一）。
- ✅ Play 模式零运行时错误。

## 下一步状态

进入迭代 5（死亡/结束循环 + 难度 + UI）。

## 备注

- **事件系统问题**：TypeEventSystem 的 `SendEvent`/`RegisterEvent` 在本项目环境未正常传播（事件发出但面板未收到），改用 Model 的 `BindableProperty`（`Level.Register`）驱动面板——更简单可靠。
- **编译时序坑**：写新 `.cs` 文件后需 `asset_reimport` 具体文件（reimport 文件夹不检测新文件）。
- 验证时临时放大拾取半径到 50 以便静止玩家拾取，验证后已恢复 0.8。
