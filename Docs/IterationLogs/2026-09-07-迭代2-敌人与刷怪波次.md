# 2026-09-07-迭代2-敌人与刷怪波次

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-07 |
| 迭代目标 | 敌人 + 刷怪波次 + 对象池 |
| 关联计划 | `Docs/Plans/2026-09-07-计划-菌核狂潮MVP架构与迭代计划.md` |

## 关键步骤

1. 写 `PoolUtility.cs`（Utility 工具层）：简单对象池（按 prefab 分池，Spawn 复用 / Despawn 回收），注册进 `GameArchitecture`。
2. 写 `EnemyController.cs`（表现层 MonoBehaviour）：向玩家直线追击，`Init(Transform player)` 注入玩家引用。
3. 写 `EnemySpawner.cs`（表现层 MonoBehaviour，`IController`）：按间隔在玩家周围刷怪，走 `GetUtility<IPoolUtility>()` 复用敌人。
4. 通过 unity-skills REST 搭场景：
   - 创建 Enemy（Quad）+ EnemyController → 转 prefab（`Assets/Prefabs/Enemy.prefab`）→ 删除场景实例。
   - 创建 EnemySpawner + EnemySpawner 组件 → 设 enemyPrefab（prefab 引用）+ player（Player 引用）。
5. 保存场景 `Main.unity`。

## 影响范围

- 新增 `Assets/Scripts/Utility/PoolUtility.cs`、`Assets/Scripts/Controller/EnemyController.cs`、`Assets/Scripts/Controller/EnemySpawner.cs`
- 修改 `GameArchitecture.cs`（注册 `IPoolUtility`）
- 新增 `Assets/Prefabs/Enemy.prefab`

## 验证结果

- ✅ 编译通过（0 错误）。
- ✅ 敌人刷出：Play 模式 `scene_find_objects` 找到 `Enemy(Clone)`。
- ✅ 敌人追击：观察到敌人已追到玩家附近（位置随时间向玩家靠近）。
- ✅ Play 模式零运行时错误。

## 下一步状态

进入迭代 3（战斗闭环：自动攻击 + 击杀 + 掉落）。

## 备注

- 波次/难度逻辑（SpawnSystem）延后到迭代 5，当前 EnemySpawner 内联简单刷怪计时。
- 编译时序坑：脚本写入后需等 Assembly-CSharp 重新编译完成，`component_add` 才能解析到类型（瞬时「IPoolUtility 找不到」的编译错误会自动消除）。
