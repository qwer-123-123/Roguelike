# 2026-09-07-迭代0-工程与架构骨架

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-07 |
| 迭代目标 | 建立 QFramework 四层架构骨架（目录结构 + 架构单例 + 启动引导） |
| 关联计划 | `Docs/Plans/2026-09-07-计划-菌核狂潮MVP架构与迭代计划.md` |

## 关键步骤

1. 从 Gitee（码云）下载 QFramework 核心 `QFramework.cs`（v1.0，2026-08-12 版，28KB，MIT 协议）到 `Assets/QFramework/`。
   - GitHub 不可达（国内网络超时），改用官方 Gitee 镜像 `gitee.com/liangxiegame/QFramework`。
   - Toolkits（ResKit/UIKit/PoolKit）按计划延后引入，MVP 阶段不导入 `.unitypackage`。
2. 建立分层目录：`Scripts/`（System / Model / Utility / Controller / Command / Event）+ `Configs/` + `Art/`。
3. 写 `GameArchitecture.cs`（`Architecture<GameArchitecture>` 子类，`Init()` 空实现 + 日志，后续迭代注册各层）。
4. 写 `GameBootstrap.cs`（`RuntimeInitializeOnLoadMethod` 引导，Play 时自动 `InitArchitecture()` + 日志）。
5. 确认渲染管线为 Built-in（manifest 无 URP），2D 物理 / Tilemap / uGUI / TMP 均可用。

## 影响范围

- 新增 `Assets/QFramework/QFramework.cs`
- 新增 `Assets/Scripts/GameArchitecture.cs`、`Assets/Scripts/GameBootstrap.cs`
- 新增目录 `Scripts/{System,Model,Utility,Controller,Command,Event}`、`Configs/`、`Art/`

## 验证结果

- ✅ 编译通过：`console_get_logs`（type=Error）返回 0 错误。
- ✅ 架构初始化：进入 Play 模式后 Console 出现两条日志：
  - `[菌核狂潮] GameArchitecture 初始化完成`（GameArchitecture.cs:15，经 `Architecture<T>.InitArchitecture()`）
  - `[菌核狂潮] 架构启动完成`（GameBootstrap.cs:16）
- ✅ 全程通过 unity-skills REST 驱动编辑器完成（editor_play / console_get_logs / editor_stop）。

## 下一步状态

迭代 0 已验证通过 → 等待用户确认后进入迭代 1（玩家移动 + 摄像机）。
