# 2026-09-24-迭代1.5-兵种选择界面

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-24 |
| 迭代目标 | 补上兵种系统的入口界面：开局时能选兵种，且 10 个兵种都能实际玩到 |
| 关联计划 | `Docs/Plans/2026-09-22-计划-兵种系统与美术落地.md`（本迭代为当日新增，见该文档迭代总览下的补注） |

## 为什么会有这个迭代

迭代 1 把兵种系统的**数据与逻辑**做完了（`SelectClassCommand`、`PlayerStatSystem`、10 个兵种配置、4 套攻击模型），但**没有任何选择入口** —— `SelectedClassIndex` 恒为 0，所以只能玩到「男性·匕首」。

而计划最初把选择界面排在很远的位置（正式文档里并进了「界面换皮」），意味着在那之前另外 9 个兵种都无法实际试玩。用户指出后改为独立迭代并提前 —— 这也正是计划「推荐推进方向」第 3 条的立场：**先能切兵种、再做界面**，否则界面做好了但逻辑没通，排查要在两层之间来回跳。

## 关键步骤

1. **新增 `GamePhase.ClassSelect` 阶段**。流程变为 `Ready →（点开始）→ ClassSelect →（选兵种 + 点出发）→ Playing`。该阶段不属于 Playing，各玩法逻辑的门禁会自动让世界静止，不需要额外处理。
2. **`StartMenuPanelController.StartGame()` 改为进入 ClassSelect**（原先直接进 Playing）。
3. **新增 `Controller/ClassSelectPanelController.cs`**：面板显隐、10 个兵种按钮、详情刷新、出发按钮。数值直接读 `IPlayerStatSystem`，保证展示的就是实际生效的值。
4. **新增 `Assets/Editor/ClassSelectPanelBuilder.cs`**：菜单 `Tools ▸ Game ▸ Build Class Select Panel`，在编辑器侧搭出完整层级 → 存成预制体 → 在场景里放实例。**满足工程约定「UI 一律用预制体、脚本只通过 `[SerializeField]` 引用、不存在代码创建的 UI」** —— 编辑器工具只在资产侧工作，不参与运行时。

## 设计决策

- **文案用 ASCII 占位**。工程尚无中文字体（策划案风险 R6），legacy `Text` 用内置 `LegacyRuntime.ttf`（只有拉丁字形），中文会渲染成豆腐块。`ClassSelectPanelController.BuildLabel()` 是唯一的文案出口，字体到位后改成 `ClassCatalog.DisplayNameAt(index)` 即可。
- **手工建 legacy `Text` 而不是用 `ui_create_text`** —— 后者在有 TMP 时会默认建 TMP 组件，而工程禁用 TMP。
- **排序值 95**，与计划的排序表一致（StartMenu 100 > ClassSelect 95 > GameOver 90 > Hud 0）。
- **面板加 `OnEnable` 刷新**：刷新幂等，被重新激活时不该显示陈旧数据。

## 影响范围

- 新增：`Controller/ClassSelectPanelController.cs`、`Editor/ClassSelectPanelBuilder.cs`、`Prefabs/ClassSelectPanel.prefab`（91 个节点）
- 修改：`Model/GameStateModel.cs`（新增阶段）、`Controller/StartMenuPanelController.cs`
- 场景：`Main.unity` 新增 `ClassSelectCanvas` 实例

## 验证结果

用 `unity-skills` 的 `event_invoke` 触发真实按钮的 `onClick`，走了一遍完整交互链路（REST 无法模拟键鼠，但能触发 UnityEvent）：

| 检查 | 结果 |
|------|------|
| 预制体结构 | 10 个 `Class0-9` + 10 个 `ClassLabel` + `Stat0-5` + `Weapon0-4` + `Title`/`Portrait`/`ClassName`/`ClassRole`/`StartButton`，共 91 节点 |
| 序列化接线 | `classButtons` 10 项 / `classButtonLabels` 10 项 / `statTexts` 6 项 / `weaponIcons` 5 项，单值引用全部非空 |
| `Canvas.sortingOrder` | 95 ✓ |
| 点击「开始游戏」 | 进入 ClassSelect，面板显示 ✓ |
| **10 个标签填充正确** | `ClassLabel0`=MAN / KNIFE、`4`=MAN / FLAME、`5`=GIRL / KNIFE、`9`=GIRL / FLAME —— **索引约定（角色为主序）正确** |
| 详情数值来自系统层 | `Stat0`="HP 120"、`Stat5`="MODEL Arc"（匕首走扇形模型）✓ |
| **点击 Class5（女性·匕首）** | `ClassName`→GIRL / KNIFE、**HP 120→100**、**SPD 3.2→3.6** —— 角色层差异正确传播 ✓ |
| 选中态 | `ClassLabel5` 变亮绿 `(0.78,0.88,0.24)`，`ClassLabel0` 转暗 `(0.65,0.7,0.78)` ✓ |
| 点击「START」 | 面板隐藏（`isActiveAndEnabled`=false）、玩家换上女性精灵集、`MaxHealth`=100 ✓ |
| **攻击模型在实际工作** | 运行态 `CharacterView.State`=`Attack`、`FrameIndex`=4/8、`AimRadians`=0.789（正在瞄准敌人）✓ |
| 运行态错误 | **0** ✓ |

运行态截图（`Assets/Screenshots/`）：玩家已是持匕首的**女性**素材，白色方块是仍为 Cube 图元的敌人。

## 遗留说明

**一个未定位的视觉现象**：玩家周围有一圈**块状白色光晕**，比角色大、随角色移动、随动画变形状。

已排除的可能：
- 不是角色自身 —— 禁用 `CharacterView` 后光晕依旧
- 不是任何 SpriteRenderer —— 全场景只有一个（`Player/Visual`）
- 不是 mipmap —— 导入设置 `mipmaps: false`、运行时 `mipmapCount: 1`
- 拆出来的原图是干净的，问题在渲染侧

**最可能的解释：敌人（仍是 Cube 图元）堆叠在玩家身上。** 白色方块彼此重叠会形成更大的不规则白块，且必然跟着玩家走、随敌人抖动而变形 —— 与观察到的四个特征全部吻合。这也正是**迭代 2 的分离力要解决的问题**（H5 原型里同一现象出现过，当时把分离系数从 0.55 调到 3.2 才散开）。

未能用 REST 确证：`gameobject_find` 找不到运行时生成的敌人实例（对象池产物），所以无法直接读到它们的坐标分布。

**验证方式**：迭代 2 接入分离力后重看这个现象是否消失；或运行时在 Hierarchy 里数一下玩家脚下的敌人数量。

**其他遗留**：
- 界面文案仍是 ASCII 占位，等用户提供中文字体
- `Assets/Configs/` 与 `Assets/Screenshots/` 与 `Assets/Art/` 同进同出（均被 `.gitignore` 忽略，待仓库改私有后一并提交）

## 下一步状态

迭代 1.5 完成，**10 个兵种现在都能实际试玩**。

**下一步是迭代 2：敌人分级 + 美术**（`EnemyDefinition` × 11、11 个敌人预制体替换 Cube、分离力、血条、受击闪白、死亡动画接 `GameOverCommand`）。

按「一个迭代一个闭环」约定，**等你确认后再开始**。建议先实机试玩一局，确认选角流程与光晕现象。
