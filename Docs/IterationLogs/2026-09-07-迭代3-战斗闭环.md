# 2026-09-07-迭代3-战斗闭环

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-07 |
| 迭代目标 | 战斗闭环（自动攻击 + 击杀 + 掉落） |
| 关联计划 | `Docs/Plans/2026-09-07-计划-菌核狂潮MVP架构与迭代计划.md` |

## 关键步骤

1. 新增 `GameStateModel.cs`（Model 数据层）：`IGameStateModel.Experience`（`BindableProperty<int>`），注册进架构。
2. 新增 `AddExperienceCommand.cs`（Command）：拾取经验时通过命令改 Model 状态（遵循「Controller 改 Model 走 Command」）。
3. 改 `EnemyController.cs`：加血量 / 受击 / 死亡（掉落经验 + 回收对象池）；维护静态活跃敌人表供玩家瞄准；改为 `IController` 以取用 `IPoolUtility`。
4. 新增 `ProjectileController.cs`：追踪目标敌人，命中扣血并回收。
5. 新增 `PlayerCombatController.cs`：自动瞄准最近敌人发射投射物（割草核心：玩家只走位，攻击自动化）。
6. 新增 `ExperienceDropController.cs`：玩家靠近拾取，加经验并回收。
7. 通过 unity-skills REST 搭场景：创建 Projectile / ExpDrop prefab，给 Player 加 PlayerCombatController（设 projectilePrefab），给 Enemy prefab 设 expDropPrefab。

## 影响范围

- 新增 `Model/GameStateModel.cs`、`Command/AddExperienceCommand.cs`、`Controller/{ProjectileController,PlayerCombatController,ExperienceDropController}.cs`
- 修改 `Controller/EnemyController.cs`、`GameArchitecture.cs`
- 新增 `Assets/Prefabs/Projectile.prefab`、`Assets/Prefabs/ExpDrop.prefab`

## 验证结果

- ✅ 编译通过（0 错误）。
- ✅ 战斗链路：Play 模式同时观察到 Enemy(Clone)（敌人）、Projectile(Clone)（投射物）、ExpDrop(Clone)（掉落）。
- ✅ Play 模式零运行时错误。

## 下一步状态

进入迭代 4（局内升级三选一）。

## 备注

- 编译时序坑：本次新文件未被 Unity 文件监听自动检测到，需用 `asset_reimport` 强制重新导入后才触发编译。后续写脚本后应主动 reimport。
