# AI协作启动工作流

> 给首次接手任务的 AI 一份最小可执行的启动说明。完整规则定义见 `AGENTS.md`。

## 1. 默认启动顺序

### 1.1 检查 Skill 体系
- 如存在 `MySkills/`，优先读 `MySkills/README.md` 与匹配的本地技能
- 涉及文档初始化优先用 `MyDocsSkill`，涉及子智能体并行优先用 `ParallelSkills`
- 如项目根目录无 `.claude/settings.json`，询问是否启用 Claude Code 自动增强

### 1.2 验证项目身份
读取 `Docs/Current/project-fingerprint.md`，确认项目根目录一致。路径不匹配立即停止。详见 `AGENTS.md §2.2`。

### 1.3 确认本轮意图
至少确认：本轮是分析/方案/执行/评审；目标是什么；是否允许直接修改。约束不清时先提问，不能直接展开完整方案。

### 1.4 建立最小上下文
按任务规模读必要信息。S 级读相关代码；M 级补读阶段和计划；L 级再补架构。项目缺 `Docs/` 骨架时自动初始化（详见 `AGENTS.md §2.4`）。

## 2. 任务分级标签

| 级别 | 特征 | 动作 |
|------|------|------|
| S | 单文件修复，无架构影响 | 直接执行 → 验证 → 可补最小日志 |
| M | 跨文件功能调整 | 先方案 → 确认 → 按迭代执行 → 每迭代补日志 |
| L | 跨模块/架构变更 | 头脑风暴 → 方案 → 计划 → 分迭代执行 |

完整分级规则与门禁见 `AGENTS.md §3`。

## 3. 方案门禁

改动跨模块、涉及边界调整、验证成本高、或关键决策不清时，必须先出方案再执行。方案至少应覆盖目标、范围、风险、验证方式和非目标。详见 `AGENTS.md §4.2`。

## 4. 文档最小集

项目应至少包含：`Docs/Current/session-active.md`（单一状态源）、`Docs/Current/project-fingerprint.md`、`Docs/Architecture/`、`Docs/Plans/`、`Docs/IterationLogs/`、`Docs/Templates/`、`Docs/Archive/`。

`Docs/` 和 `.claude/` 由初始化流程自动生成，不要求手动复制。详见 `AGENTS.md §5`。

## 5. 验证闭环

完成声明前必须给出至少一项：编译通过 / 测试通过 / 运行态验证通过 / 无法验证的原因。详见 `AGENTS.md §4.4`。

## 6. 禁止项

- 未理解任务就开始改
- 用户未确认就做主方案决策
- 跳过验证宣称完成
- 计划写完自动开始迭代
- 迭代日志用一句话替代独立文件
- 把临时修补当成最终架构
